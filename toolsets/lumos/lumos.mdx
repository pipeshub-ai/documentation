---
title: "Lumos Toolset"
description: "Access requests, policies, and app permissions for AI agents"
icon: "/logo/lumos.png"
---

<div className="not-prose mt-10">
  <div className="p-6 border border-gray-200 dark:border-gray-700 rounded-xl bg-blue-50 dark:bg-gray-800">
    <div className="flex items-center gap-3 mb-4">
      <img
        src="/logo/lumos.png"
        alt="Lumos Logo"
        className="w-8 h-8 object-contain flex-shrink-0"
      />
      <div className="text-xl font-semibold text-gray-900 dark:text-white">Lumos Toolset</div>
    </div>
    <p className="text-base text-gray-700 dark:text-gray-300 mt-0 mb-5">Access requests, policies, and app permissions</p>
    <div className="flex flex-wrap items-center gap-2">
      <span className="px-3 py-1.5 bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200 rounded-full text-sm font-medium">
        Ready
      </span>
      <span className="px-3 py-1.5 bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 rounded-full text-sm font-medium">
        Identity
      </span>
    </div>
  </div>
</div>

## Overview

The Lumos toolset lets PipesHub agents act in Lumos rather than only read from it. It exposes **26 tools**.

If you want Lumos content to be searchable instead, you need a [connector](/connectors/overview). Connectors index data; toolsets perform actions. Many teams use both.

## Setting it up

Toolsets authorise separately from connectors, even where you already have a connector for the same service. A connector indexes data for search; a toolset lets an agent act.

1. Create an API credential in the provider's own console, scoped to the least access the agent needs.
2. In PipesHub, go to **Workspace settings → Actions** and find this integration.
3. Select **Setup**, paste the credential, and save.

<Note>
An agent can do anything the credential allows. Prefer a dedicated, least-privilege credential over reusing an administrator's.
</Note>

## Available Tools

| Tool | What it does |
|------|--------------|
| `lumos.add_user_role` | Add a role to a Lumos user |
| `lumos.cancel_access_request` | Cancel a Lumos access request |
| `lumos.create_access_policy` | Create a Lumos access policy |
| `lumos.create_access_request` | Create a Lumos access request |
| `lumos.create_requestable_permission` | Create a requestable permission in Lumos |
| `lumos.delete_access_policy` | Delete a Lumos access policy |
| `lumos.delete_requestable_permission` | Delete a requestable permission in Lumos |
| `lumos.get_access_policy` | Get a Lumos access policy by ID |
| `lumos.get_access_request` | Get a Lumos access request by ID |
| `lumos.get_group` | Get a Lumos group by ID |
| `lumos.get_group_members` | Get members of a Lumos group |
| `lumos.get_platform` | Get a Lumos platform by ID |
| `lumos.get_requestable_permission` | Get a requestable permission by ID |
| `lumos.get_user` | Get a Lumos user by ID |
| `lumos.get_user_accounts` | Get accounts assigned to a Lumos user |
| `lumos.get_user_roles` | Get roles assigned to a Lumos user |
| `lumos.list_access_policies` | List Lumos access policies |
| `lumos.list_access_requests` | List Lumos access requests |
| `lumos.list_app_permissions` | List requestable permissions for a specific Lumos app |
| `lumos.list_groups` | List Lumos groups |
| `lumos.list_permissions` | List Lumos requestable permissions |
| `lumos.list_platforms` | List Lumos platforms (apps) |
| `lumos.list_users` | List Lumos users |
| `lumos.remove_user_role` | Remove a role from a Lumos user |
| `lumos.update_access_policy` | Update a Lumos access policy |
| `lumos.update_requestable_permission` | Update a requestable permission in Lumos |

## Using it in an agent

1. Open **Agents** and create or edit an agent.
2. From the **Tools** panel, drag this toolset onto the canvas and connect it to the Agent Core.
3. To let the same agent also search indexed content, add a node from the **Knowledge** panel and connect that too.

The agent chooses which tool to call from the descriptions above, so a clear instruction such as "create an issue titled X" routes better than a vague one.

## Related

- [Toolsets overview](/toolsets/overview)
- [Lumos API documentation](https://developers.lumos.com/)
